%article.video_category
  %header
    %h3= @category.name
  %article.row
    - @category.videos.each do |video|
      .video.col-md-2
        = link_to video_path(video.id) do 
          %img(src="/tmp/monk.jpg")